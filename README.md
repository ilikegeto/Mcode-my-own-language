# PipaLang-my-own-language
this is my own language still dosen't have many feature but it's working
basic vvvvvvvvvvvvv 
print vvv
 - value.scr(); <- print text or value
 - value.scr_nextl(); <- like value.scr but can enter without "\n'
 - value.replace(string name, "text you want to change", "replace text");
 - value.in(variable name);
variables vvv
 - str myString = "hello world"; <- str for string 
 - str hello;                    </
 - int myNumber = 12345; <- int for int or you can say it number
 - int number;           </
 - bool myBool = true;     <- bool for boolean 
 - bool notMyBool = false; </
loop vvv
 - for(int i = 0; i < variable; i++) {} <- for loop base on C++
 - while(i < variable) {} <- while loop base on C++ too
if/elif/else
 - if(condition) {} <- if statement
 - elif(condition) {} <- else if statement
 - else {} <- else statement
array vvv
 - array_str myStringArray = {}; <- array for string
 - array_int myIntArray = {}; <- array for int
 - myStringArray.pushitem(); <- array push_back()
